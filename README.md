# restack
Minimal, zero-setup CLI for stacked PRs. Auto-detects the stack from your branches, rebases changes up it, and cleans up after squash-merges. Just a bash script, with autocomplete and a SKILL.md if you want that.
